# Character vectors - tolower(), toupper()

ambulanceData <- read.csv("C:/Users/Open User/Desktop/Muideen_SAIL/nigeria_ambulances.csv")

head(ambulanceData)

# function to get or set the names of an Object
names(ambulanceData)

# Character Transission and case folding
tolower(names(ambulanceData))
